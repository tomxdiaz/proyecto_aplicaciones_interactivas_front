import { Input as InputMui, styled } from '@mui/material';

export const Input = styled(InputMui)({
  border: '2px solid black',
  height: '2rem',
  borderRadius: '10px',
  textAlignLast: 'center'
});
